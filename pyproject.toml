[project]
name = "aprilalgo"
version = "0.1.0"
description = "A Python data science and analysis project"
readme = "README.md"
license = { file = "LICENSE" }
requires-python = ">=3.11"
dependencies = [
    "jupyter>=1.1.1",
    "matplotlib>=3.10.8",
    "numpy>=2.4.4",
    "pandas>=3.0.1",
]

[tool.uv]
dev-dependencies = []
